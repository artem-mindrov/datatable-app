<h1>Listing items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Level</th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.level %></td>
  </tr>
<% end %>
</table>

<br />
